<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.coding.guide.mapper.QuestionMapper">

    <sql id="field01">
               id,
               title,
               recommend,
               read_count,
               like_count,
               collect_count,
               meet_count,
               difficulty,
               tags,
               publish_time
    </sql>

    <select id="selectHottestQuestionByLimit" resultType="com.coding.guide.entity.Question">
        SELECT
        <include refid="field01"/>
        FROM `t_question`
        WHERE del_flag = 0
          AND is_public = 1
        ORDER BY read_count DESC, like_count DESC, collect_count DESC, comment_count DESC
        LIMIT #{page},#{size}

    </select>



</mapper>